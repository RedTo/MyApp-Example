import 'package:flutter_test/flutter_test.dart';
import 'package:myapp/util/dataProviders.dart';

void main() {
  //test('ContactProvider default test', () async {
  //  await ContactProvider().open();
  //  var contacts = await ContactProvider().getContacts();
  //
  //  expect(contacts.length, 0);
  //});
}
